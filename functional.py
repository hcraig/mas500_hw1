# Print out total vote counts for each person# Showing the "functional" stylef = open('2012_US_election_state.csv', 'r')print "Opened file:"all_lines = f.readlines()votes = [line.split(",")[3] for line in all_lines]votes.pop(0)total_votes = 0for vote in votes:	total_votes += int(vote)print "Total Obama votes: " + str(total_votes)votes_romney = [line.split(",")[5] for line in all_lines]votes_romney.pop(0)total_votes_romney = 0for vote in votes_romney:	total_votes_romney += int(vote)print "Total Romney votes: " + str(total_votes_romney)